def process_image(image_path: str): 
    
    # converts raw image path to a numpy array, supported file types: JPEG, PNG, BMP, TIFF
    
    # for JPG, PNG, BMP, directly use Image from PIL 
    
    
    # for TIFF, use cv2.imread() to read the image and convert it to RGB format
    
    image_data = ''
    return image_data 